apiVersion: v1
kind: Pod
metadata:
  name: main-nginx
  namespace: edu-azad-salimof
  labels:
    app: main-nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.16.1
      ports:
        - containerPort: 80
          protocol: TCP
      volumeMounts:
        - name: nginx-config
          readOnly: true
          mountPath: /etc/nginx/nginx.conf
          subPath: nginx.conf
  volumes:
    - name: nginx-config
      configMap:
        name: main-nginx-config
        items:
          - key: nginx.conf
            path: nginx.conf
  restartPolicy: Always